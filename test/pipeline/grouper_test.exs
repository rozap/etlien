defmodule GrouperTest do
  use ExUnit.Case, async: false
  # alias Etlien.Transform.Grouper
  # alias Etlien.Set

  # import Ecto.Query
  # alias Etlien.Ref
  # alias Etlien.Repo

  # setup_all do
  #   {:ok, pid} = Etlien.Persist.start_link
  #   on_exit fn -> Process.exit(pid, :kill) end
  #   :ok
  # end


end